##include <stdio.h>
int main(){

    int n, year, choice = 0;
    float val, tag, deprec;
    while (choice != 4){
        printf('\nMethod: (1-SL 2-DDB  3-SYD 4-End) '); 
        scanf("%d", &choice);
        if (choice >= 1 && choice <= 3){
            printf("0riginal value: ");
            scanf ( "%f &val) ;
        }
    }
} 